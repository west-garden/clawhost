apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clawhost.fullname" . }}
  labels:
    {{- include "clawhost.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicas }}
  selector:
    matchLabels:
      {{- include "clawhost.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "clawhost.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "clawhost.fullname" . }}
      {{- if .Values.postgresql.enabled }}
      initContainers:
        - name: wait-for-pg
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z {{ include "clawhost.fullname" . }}-pg 5432; do echo "waiting for postgres..."; sleep 2; done']
      {{- end }}
      containers:
        - name: clawhost
          image: "{{ .Values.server.image.repository }}:{{ .Values.server.image.tag }}"
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.server.port }}
          env:
            - name: CLAWHOST_API_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "clawhost.fullname" . }}
                  key: admin-token
            - name: CLAWHOST_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "clawhost.fullname" . }}
                  key: db-password
          volumeMounts:
            - name: config
              mountPath: /app/config.toml
              subPath: config.toml
            - name: shared-data
              mountPath: {{ .Values.storage.basePath }}
          resources:
            {{- toYaml .Values.server.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.server.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.server.port }}
            initialDelaySeconds: 3
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: {{ include "clawhost.fullname" . }}-config
        - name: shared-data
          hostPath:
            path: {{ .Values.storage.hostPath }}
            type: DirectoryOrCreate
